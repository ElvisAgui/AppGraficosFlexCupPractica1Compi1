<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.compiladores.graficoslexcup">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GraficosLexCup">
        <activity
            android:name=".ReportError"
            android:exported="false" />
        <activity
            android:name=".PantallasDeGraficos"
            android:exported="false" />
        <activity
            android:name=".PantallasGraficas"
            android:exported="false"
            android:label="@string/title_activity_pantallas_graficas"
            android:theme="@style/Theme.GraficosLexCup.NoActionBar" />
        <activity
            android:name=".PantallaMenu"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>